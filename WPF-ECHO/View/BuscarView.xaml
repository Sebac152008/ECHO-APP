<UserControl x:Class="WPF_ECHO.View.BuscarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ECHO.View"
             mc:Ignorable="d"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="#000"
             FontFamily="{DynamicResource MaterialDesignFont}"
             d:DesignHeight="700" d:DesignWidth="1180">

    <materialDesign:DialogHost Identifier="MainDialogHost">

        <Grid>


            <Image x:Name="FondoImagen" Loaded="Image_Loaded" Stretch="UniformToFill" Opacity="0.5" />

            <StackPanel Panel.ZIndex="50" x:Name="StackMensajes" VerticalAlignment="Top" Width="220" Margin="10" Orientation="Vertical"/>


            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>


                <Grid Panel.ZIndex="20" Grid.Row="0" Margin="40,40,40,0">

                    <Border Background="#F2F4F5"
                            CornerRadius="20"
                            Padding="20,10"
                            Effect="{StaticResource MaterialDesignElevationShadow2}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <fa:IconImage Icon="Search"
                          Width="22"
                          Height="22"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"
                          Foreground="#7F8C8D"
                          Grid.Column="0" />

                            <TextBox x:Name="txtBuscarRecordatorio"
                     Grid.Column="1"
                     BorderThickness="0"
                     Background="Transparent"
                     FontSize="16"
                     Foreground="#2C3E50"
                     VerticalContentAlignment="Center"
                     Padding="5"
                     Height="40"
                     Style="{StaticResource MaterialDesignTextBoxBase}"
                     materialDesign:HintAssist.Hint="Buscar recordatorio..."
                     TextChanged="txtBuscarRecordatorio_TextChanged" />
                        </Grid>
                    </Border>
                </Grid>



                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Grid.Row="1" Margin="0,20,0,0" Padding="0 0 0 0" >
                    <StackPanel x:Name="StackResultados" Margin="30 0 30 0" />
                </ScrollViewer>

            </Grid>

        </Grid>

    </materialDesign:DialogHost>
    
</UserControl>
