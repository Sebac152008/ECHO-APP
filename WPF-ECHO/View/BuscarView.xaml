<UserControl x:Class="WPF_ECHO.View.BuscarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ECHO.View"
             mc:Ignorable="d"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
             d:DesignHeight="700" d:DesignWidth="1180">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="130" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="40,20,40,0" HorizontalAlignment="Center">
            <Border Background="#F2F4F5"
            CornerRadius="30"
            Padding="35, 25"
            Effect="{StaticResource MaterialDesignElevationShadow4}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <fa:IconImage Icon="Search"
                          Width="25"
                          Height="25"
                          VerticalAlignment="Center"
                          Foreground="#7F8C8D"
                          Grid.Column="0" />

                    <TextBox x:Name="txtBuscarRecordatorio"
                     Grid.Column="1"
                     BorderThickness="0"
                     Background="Transparent"
                     MaxLength="50"
                     MinWidth="500"
                     HorizontalContentAlignment="Left"
                     FontSize="16"
                     Height="40"
                     Foreground="#2C3E50"
                     Padding="10 0"
                     VerticalContentAlignment="Center"
                     TextChanged="txtBuscarRecordatorio_TextChanged"
                     materialDesign:HintAssist.Hint="Buscar recordatorio..."
                     Style="{StaticResource MaterialDesignTextBoxBase}" />
                </Grid>
            </Border>
        </StackPanel>



        <ScrollViewer Grid.Row="1" Margin="40,20,40,40">
            <StackPanel x:Name="StackResultados" />
        </ScrollViewer>

    </Grid>
</UserControl>
