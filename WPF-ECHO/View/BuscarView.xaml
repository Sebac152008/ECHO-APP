<UserControl x:Class="WPF_ECHO.View.BuscarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ECHO.View"
             mc:Ignorable="d"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
             d:DesignHeight="700" d:DesignWidth="1180">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border CornerRadius="20" Background="#2C3E50" Height="80" Padding="0 10 0 10" Margin="40 0 40 0" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Ícono ocupa solo lo necesario -->
                    <ColumnDefinition Width="*"/>
                    <!-- TextBox ocupa todo el resto -->
                </Grid.ColumnDefinitions>

                <fa:IconImage Icon="Search" Width="40" Margin="50 0 20 0" Foreground="White" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtBuscarRecordatorio" GotFocus="txtBuscarRecordatorio_GotFocus" LostFocus="txtBuscarRecordatorio_LostFocus" Text="Buscar recordatorio..." Foreground="#A2AAB2" FontSize="20" MaxLength="50" BorderThickness="0" Height="40" BorderBrush="White" VerticalAlignment="Center" Margin="0 0 20 0" Grid.Column="1"/>
            </Grid>
        </Border>


    </Grid>
</UserControl>
