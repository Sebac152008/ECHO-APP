<UserControl x:Class="WPF_ECHO.View.AcercaDeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_ECHO.View"
             mc:Ignorable="d" 
             d:DesignHeight="1500" d:DesignWidth="1180"
             Background="#000">

    <UserControl.Resources>

        <!-- Animacion de entrada -->

        <Storyboard x:Key="VentanaAbrirAnimacion">
            <!-- Animación de Opacidad (fade-in) -->
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.4"/>

            <!-- Animación de desplazamiento vertical -->
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="30" To="0" Duration="0:0:0.4" />
        </Storyboard>

        <!-- Títulos -->
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E1E1E"/>
            <Setter Property="Margin" Value="0 0 0 8"/>
        </Style>

        <!-- Texto normal -->
        <Style x:Key="BodyText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
        </Style>

        <!-- Tarjeta estilo neumorphism claro, para la tarjeta -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0 0 0 30"/>
            <Setter Property="Background" Value="#f4f4f4"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#888888" BlurRadius="10" ShadowDepth="2" Direction="320" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid RenderTransformOrigin="0.5,0.5">

        <!-- Imagen de fondo translúcida -->
        <Image x:Name="FondoImagen"
               Stretch="UniformToFill"
               Opacity="0.4"/>

        <!-- Contenido principal -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Panel.ZIndex="1">
            <StackPanel Margin="80 40 80 40" Orientation="Vertical">

                <!-- Logo -->
                <Image Source="/Imagenes/ECHOLOGO2.png"
                       Loaded="Image_Loaded"
                       Width="200"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 40"/>

                <!-- Secciones en tarjetas -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="¿Qué es ECHO?" />
                        <TextBlock Style="{StaticResource BodyText}" Text="ECHO es una aplicación de recordatorios inspirada en el eco: un sonido que regresa cuando más lo necesitas. El propósito es ayudarte a no olvidar tus tareas importantes y tener todo bajo control."/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="Función" />
                        <TextBlock Style="{StaticResource BodyText}" Text="ECHO te permite crear recordatorios personalizados con fecha y hora, y recibir notificaciones justo cuando lo necesitas. Además, puedes organizar tus tareas de manera eficiente y visual, para reducir el estrés y mantener tu día bien estructurado."/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="Objetivo" />
                        <TextBlock Style="{StaticResource BodyText}" Text="El objetivo principal de ECHO es proporcionar una herramienta práctica y accesible para todos los usuarios, desde estudiantes hasta profesionales, para mejorar su organización personal y aumentar su productividad en el día a día."/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="Soporte" />
                        <TextBlock Style="{StaticResource BodyText}">
                            <Run Text="Correo: " />
                            <Run Text="sebac152008@gmail.com" FontWeight="Bold" Foreground="#2A6AC3"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource BodyText}">
                            <Run Text="Teléfono: " />
                            <Run Text="+1 (849)-447-1528" FontWeight="Bold" Foreground="#2A6AC3"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="Equipo de Desarrollo" />
                        <TextBlock Style="{StaticResource BodyText}" Text="• Mejia Gomez Sophy Marie"/>
                        <TextBlock Style="{StaticResource BodyText}" Text="• Valenzuela De Paula Angel Sebastian"/>
                        <TextBlock Style="{StaticResource BodyText}" Text="• Cuevas Santiago Sebastian"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="Versión" />
                        <TextBlock Style="{StaticResource BodyText}" Text="ECHO v1.0 – Mayo 2025"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Margin="0">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleText}" Text="Licencia" />
                        <TextBlock Style="{StaticResource BodyText}" Text="Este software fue desarrollado como parte de un proyecto académico. Todos los derechos son reservados por los autores. Uso educativo."/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
