<UserControl x:Class="ECHO.View.EditarRecordatorioDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ECHO.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Width="300" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Titulo" FontSize="15" FontWeight="Bold"/>
        <TextBox x:Name="txtNotaEditar" Margin="0,20,0,0" materialDesign:HintAssist.Hint="Nota"/>
        <TextBlock Grid.Row="1" Text="Fecha" Margin="0 5 0 0" FontSize="15" FontWeight="Bold" />
        <DatePicker x:Name="fechaEditar" Grid.Row="1" Margin="0,30,0,0"/>
        <TextBlock Grid.Row="2" Text="Hora" Margin="0 5 0 0" FontSize="15" FontWeight="Bold" />
        <materialDesign:TimePicker x:Name="horaEditar" Grid.Row="2" Margin="0,30,0,0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar"
                Margin="0,0,10,0"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="false"/>
            <Button Content="Guardar"
                Background="#27AE60"
                Foreground="White"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="true"/>
        </StackPanel>
    </Grid>
</UserControl>
